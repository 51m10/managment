name: Build Flet APK

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.x'
          channel: 'stable'
          cache: true

      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install flet openpyxl

      - name: Accept Android Licenses
        run: yes | flutter doctor --android-licenses || true

      - name: Build APK
        run: |
          cd src
          flet build apk --yes

      - name: Find and Move APK
        run: |
          # پیدا کردن فایل APK در کل پروژه و انتقال آن به یک پوشه مشخص
          mkdir -p final_apk
          find . -name "*.apk" -exec cp {} final_apk/ \;

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: src/final_apk/*.apk
